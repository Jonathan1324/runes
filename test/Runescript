#include "config.rune"

spell main {
    $mkdir ${BUILD_DIR}

    cast compile SRC=${SRC_DIR}/test.c, OUT=${BUILD_DIR}/test

    $chmod +x ${BUILD_DIR}/test
}

spell compile {
    $${CC} ${CFLAGS} ${SRC} -o ${OUT}
}

spell clean {
    $rm -rf ${BUILD_DIR}
}